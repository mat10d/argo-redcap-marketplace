{
  "name": "argo-data",
  "version": "0.1.0",
  "description": "Data management for ARGO REDCap projects — the token-holding role. Export and import records, metadata, files, and audit logs via the API (data-export), and link records across studies/sources with safe diff-only write-back (study-linkage). Distinct from argo-analysis, which works on local exports without API access.",
  "author": { "name": "ARGO" }
}

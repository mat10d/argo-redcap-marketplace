# Verification

The checklist lives inside argo-core so it ships with the plugin, where a fresh session can
actually see it:

    plugins/argo-core/skills/redcap-api/references/verify-install.md

It's referenced elsewhere as [[verify-install]].
